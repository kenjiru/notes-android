<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <EditTextPreference
        android:key="pref_notesFolder"
        android:title="@string/pref_notesFolder"
        android:defaultValue="@string/pref_notesFolder_default"
        android:singleLine="true"/>

    <!-- This preference acts as a button. -->
    <Preference
        android:key="@string/dropbox_user"
        android:title="@string/pref_dropboxUser"
        android:summary="@string/not_logged_in"/>

</PreferenceScreen>
